# Copy this file to .env and adjust the values for your local MySQL/WAMP setup.
# .env is git-ignored: never commit real credentials.
DB_HOST=localhost
DB_NAME=test
DB_USER=root
DB_PASS=root
DB_CHARSET=utf8mb4
